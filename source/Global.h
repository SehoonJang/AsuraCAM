#pragma once

#include <windows.h>
#include <tchar.h>
#include <stdio.h>
#include <strsafe.h>

#include "SimConnect.h"

enum GROUP_ID {
    GROUP0,
};

enum EVENT_ID {
    EVENT_CAMERA_RIGHT,
    EVENT_CAMERA_LEFT,
};

enum INPUT_ID {
    INPUT0,
};